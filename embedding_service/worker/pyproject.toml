[project]
name = "worker"
version = "0.1.0"
description = ""
authors = [
    {name = "TechnoDamo",email = "technobladedamir@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "grpcio (>=1.76.0,<2.0.0)",
    "grpcio-tools (>=1.76.0,<2.0.0)",
    "pydantic-settings (>=2.12.0,<3.0.0)",
    "structlog (>=25.5.0,<26.0.0)",
    "prometheus-client (>=0.24.1,<0.25.0)",
    "psutil (>=7.2.1,<8.0.0)",
    "uvloop (>=0.22.1,<0.23.0)",
    "torch (>=2.0.0,<3.0.0)",
    "transformers (>=4.30.0,<5.0.0)",
    "tokenizers (>=0.15.0,<0.16.0)",
    "numpy (>=1.24.0,<2.0.0)",
    "sentencepiece (>=0.2.0,<0.3.0)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
