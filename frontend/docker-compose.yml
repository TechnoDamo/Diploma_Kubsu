services:
  frontend:
    build:
      context: .
    container_name: rag-system-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_HOST_PORT:-3001}:${FRONTEND_CONTAINER_PORT:-3000}"
    networks:
      - rag-system
